# 📝 面向对象程序设计 全面测评卷

> 本卷共 100 题，其中选择题 70 题，填空题 30 题，覆盖所有章节。

---

## 一、选择题（共 70 题）

### ● 绪论与基本概念（1-10 题）

**第1题** 面向过程程序设计关注的是：
A. 谁来做
B. 怎么做
C. 对象之间的关系
D. 数据封装

---

**第2题** 面向对象方法的出发点是：
A. 编写高效的函数
B. 更直接地描述客观世界中的事物及其关系
C. 减少内存占用
D. 提高编译速度

---

**第3题** 以下哪项不是面向对象程序设计的基本特征？
A. 抽象性
B. 封装性
C. 递归性
D. 多态性

---

**第4题** 在面向对象方法中，"对象"由哪两部分构成？
A. 函数和变量
B. 属性和行为
C. 输入和输出
D. 基类和派生类

---

**第5题** 类与对象的关系，最恰当的比喻是：
A. 汽车与轮胎
B. 蓝图与房屋
C. 树干与树枝
D. 河流与水滴

---

**第6题** 以下哪个是面向过程的代表性语言？
A. C++
B. Java
C. C
D. Python

---

**第7题** 在面向对象程序设计中，对象之间的联系是通过什么实现的？
A. 全局变量
B. 消息传递
C. 函数重载
D. 指针运算

---

**第8题** "一个接口，多种状态"描述的是：
A. 继承
B. 封装
C. 多态
D. 抽象

---

**第9题** 下面关于消息的描述，错误的是：
A. 同一对象可接收不同形式的多个消息
B. 相同形式的消息发送给不同对象，响应可以截然不同
C. 消息的发送可以不考虑具体的接收者
D. 对象必须对收到的消息做出响应

---

**第10题** 面向对象方法的优点不包括：
A. 可重用性
B. 可维护性
C. 可扩充性
D. 运行速度更快

---

### ● 类与对象（11-25 题）

**第11题** 在类中，关键字 ______ 后面声明的成员是类与外部的接口。
A. private
B. protected
C. public
D. static

---

**第12题** 类中私有成员的特点，正确的是：
A. 类内类外均可访问
B. 只有本类成员函数和友元可以访问
C. 派生类可以访问
D. 任何函数都可以访问

---

**第13题** 关于构造函数，下列说法正确的是：
A. 构造函数可以有返回值
B. 构造函数不能被重载
C. 构造函数名必须与类名相同
D. 构造函数必须显式定义

---

**第14题** 如果没有显式定义构造函数，系统会：
A. 报错
B. 自动生成一个带参数的构造函数
C. 自动生成一个默认构造函数，不做初始化
D. 随机生成一个构造函数

---

**第15题** 以下关于析构函数的描述，错误的是：
A. 析构函数没有参数
B. 析构函数不能重载
C. 析构函数可以有返回值
D. 一个类有且仅有一个析构函数

---

**第16题** 析构函数名由什么组成？
A. `@` + 类名
B. `~` + 类名
C. `!` + 类名
D. `#` + 类名

---

**第17题** 构造函数初始化列表的写法是：
A. `Date(int y, int m, int d) : year(y), month(m), day(d) {}`
B. `Date(int y, int m, int d) { year=y; month=m; day=d; }`
C. `Date(int y, int m, int d) = year(y), month(m), day(d)`
D. `Date(int y, int m, int d) :: year(y), month(m), day(d)`

---

**第18题** 当用 `delete` 释放动态对象时，系统会：
A. 释放内存，但不调用析构函数
B. 先调用析构函数，再释放内存
C. 只调用析构函数
D. 什么也不做

---

**第19题** 静态局部对象（static属性）在什么时候调用析构函数？
A. 函数调用结束时
B. 对象被删除时
C. main函数结束或调用exit时
D. 永远不会调用

---

**第20题** 关于对象的组合（组合类），构造函数调用顺序是：
A. 本类构造函数 -> 内嵌对象构造函数
B. 内嵌对象构造函数（按声明顺序）-> 本类构造函数
C. 按内嵌对象参数列表顺序调用
D. 同时调用

---

**第21题** 组合类的析构函数调用顺序与构造函数：
A. 完全相同
B. 完全相反
C. 无关
D. 随机

---

**第22题** 前向引用声明可以用于：
A. 声明该类的对象
B. 在内联成员函数中使用该类的对象
C. 声明该类的指针或引用
D. 定义该类的内联成员函数

---

**第23题** 类中成员互访时，可以直接使用：
A. 对象名.成员名
B. 成员名
C. 类名::成员名
D. this->成员名

---

**第24题** 构造函数可以被重载，这意味着：
A. 一个类可以有多个不同名的构造函数
B. 一个类可以有多个参数个数或类型不同的构造函数
C. 构造函数可以有返回值
D. 构造函数必须手动调用

---

**第25题** 以下对构造函数的描述，错误的是：
A. 构造函数不能为虚函数
B. 构造函数可以是内联函数
C. 构造函数可以有默认形参值
D. 构造函数可以定义为 private

---

### ● 数据共享与保护（26-40 题）

**第26题** 静态数据成员的特点，正确的是：
A. 属于每个对象各自一份
B. 属于整个类，所有对象共享
C. 在构造函数中初始化
D. 不能被修改

---

**第27题** 静态数据成员初始化的正确方式是：
A. 在构造函数中初始化
B. 在类定义中直接赋值
C. 在类外，对象生成之前初始化
D. 在main函数中初始化

---

**第28题** 静态成员函数的特点是：
A. 有this指针
B. 可以访问非静态成员
C. 没有this指针，只能访问静态成员
D. 只能通过对象名调用

---

**第29题** 静态成员函数的访问格式，错误的是：
A. `类名::静态成员函数名(实参表)`
B. `对象名.静态成员函数名(实参表)`
C. `静态成员函数名(实参表)`
D. 以上都可以

---

**第30题** 下面关于友元的描述，错误的是：
A. 友元可以访问类的私有成员
B. 友元关系可以被继承
C. 友元关系是单向的
D. 友元关系不具有传递性

---

**第31题** 友元的正确使用可以：
A. 提高程序的运行效率
B. 增强封装性
C. 提高数据隐藏性
D. 增强可维护性

---

**第32题** 友元的类型包括：
A. 友元函数和友元类
B. 友元函数和友元对象
C. 友元类和友元对象
D. 友元函数和友元指针

---

**第33题** 常量引用所引用的对象：
A. 可以被修改
B. 不能被修改
C. 可以重新赋值
D. 可以指向其他变量

---

**第34题** `const int *p;` 声明的是：
A. 指向常量的指针
B. 常量指针
C. 指向指针的常量
D. 常量指针常量

---

**第35题** `int * const p = &x;` 声明的是：
A. 指向常量的指针
B. 常量指针（指针本身不能改变）
C. 指向指针的指针
D. 常量引用

---

**第36题** 友元函数不是当前类的成员函数，它访问类的成员时需要：
A. 直接访问
B. 通过对象名访问
C. 通过类名访问
D. 通过this指针访问

---

**第37题** 下面哪一个能实现数据共享？
A. 全局变量
B. 类的静态成员
C. 通过引用传递的数据
D. 以上都是

---

**第38题** 关于类作用域，以下说法正确的是：
A. 类作用域作用于所有函数
B. 类X的成员M只能通过对象名访问
C. 在X的成员函数中如果没有声明同名标识符，可以直接访问成员M
D. 类作用域和文件作用域相同

---

**第39题** 对于两个嵌套的作用域，如果内层声明了与外层同名的标识符，则：
A. 内外层都可见
B. 外层标识符在内层不可见
C. 内层标识符不可见
D. 编译报错

---

**第40题** 静态数据成员在内存中的存储位置：
A. 和普通数据成员一样在对象中
B. 单独分配存储区域
C. 在栈中
D. 在寄存器中

---

### ● this 指针与成员指针（41-45 题）

**第41题** this 指针隐含于：
A. 全局函数中
B. 每一个类的成员函数中
C. 仅静态成员函数中
D. 友元函数中

---

**第42题** 以下关于 this 指针的描述，正确的是：
A. this 指针可以显式修改
B. 静态成员函数也有 this 指针
C. this 指针指向当前操作的数据所属的对象
D. this 指针的类型是 void*

---

**第43题** 声明指向类的公有数据成员的指针，格式为：
A. `类型名 *指针名`
B. `类型名 类名::*指针名`
C. `类型名::*指针名`
D. `类名.*指针名`

---

**第44题** 通过对象名与成员指针结合访问数据成员的运算符是：
A. `.*`
B. `->*`
C. `::*`
D. `.*` 和 `->*`

---

**第45题** 指向类的公有函数成员的指针初始化方式为：
A. `指针名 = &类名::函数成员名`
B. `指针名 = 类名::函数成员名`
C. `指针名 = 对象名.函数成员名`
D. `指针名 = 函数成员名`

---

### ● 继承与派生（46-55 题）

**第46题** 继承的目的和派生的目的分别是：
A. 继承为代码重用，派生为功能扩充
B. 继承为功能扩充，派生为代码重用
C. 都是代码重用
D. 都是功能扩充

---

**第47题** 在公有继承中，基类的 public 成员在派生类中变为：
A. private
B. protected
C. public
D. 不可访问

---

**第48题** 在公有继承中，基类的 private 成员在派生类中：
A. 变为 public
B. 变为 protected
C. 保持 private
D. 不可访问

---

**第49题** 派生类构造函数执行顺序，口诀是：
A. 先自己，再客人，后祖先
B. 先祖先，再客人，后自己
C. 先祖先，再自己，后客人
D. 先客人，再祖先，后自己

---

**第50题** 多继承时，如果有多个基类，基类构造函数的调用顺序是：
A. 按照派生类声明时继承的顺序（从左到右）
B. 按照初始化列表中出现的顺序
C. 按照基类名的大小顺序
D. 随机顺序

---

**第51题** 若基类中声明了带参数的构造函数，则派生类构造函数必须：
A. 省略基类构造函数的调用
B. 在初始化列表中显式调用基类构造函数
C. 不定义构造函数
D. 使用默认构造函数

---

**第52题** 派生类的析构函数执行顺序是：
A. 基类 -> 内嵌对象 -> 派生类
B. 派生类 -> 内嵌对象 -> 基类
C. 内嵌对象 -> 基类 -> 派生类
D. 基类 -> 派生类 -> 内嵌对象

---

**第53题** 关于继承中的拷贝构造函数，以下说法正确的是：
A. 派生类拷贝构造函数必须显式调用基类拷贝构造函数
B. 若调用缺省拷贝构造函数，系统自动调用基类的缺省拷贝构造函数
C. 拷贝构造函数不能被继承
D. 派生类不能有拷贝构造函数

---

**第54题** 下面哪种关系适合用继承表示？
A. 学生-班级
B. 汽车-轮胎
C. 学生-研究生
D. 公司-员工

---

**第55题** 多重继承是指：
A. 一个基类有多个派生类
B. 一个派生类有多个直接基类
C. 多个派生类有多个基类
D. 派生类有多层继承关系

---

### ● 多态性（56-65 题）

**第56题** 以下哪种多态是运行时多态？
A. 函数重载
B. 运算符重载
C. 包含多态（虚函数）
D. 参数多态（模板）

---

**第57题** 实现动态多态需要三个条件，以下哪个不是？
A. 基类中将函数声明为虚函数
B. 公有继承（is-a关系）
C. 使用派生类指针指向基类对象
D. 通过基类指针或引用调用虚函数

---

**第58题** 虚函数用哪个关键字说明？
A. static
B. const
C. virtual
D. override

---

**第59题** 以下哪个函数不能声明为虚函数？
A. 析构函数
B. 普通成员函数
C. 构造函数
D. 以上都可以

---

**第60题** 纯虚函数的定义形式是：
A. `virtual 返回值 函数名(参数表) {}`
B. `virtual 返回值 函数名(参数表) = 0;`
C. `virtual 返回值 函数名(参数表);`
D. `返回值 函数名(参数表) = 0;`

---

**第61题** 包含至少一个纯虚函数的类称为：
A. 虚基类
B. 抽象类
C. 具体类
D. 静态类

---

**第62题** 关于抽象类，以下说法正确的是：
A. 可以创建抽象类的对象
B. 可以声明抽象类的指针或引用
C. 抽象类不能用作基类
D. 抽象类可以实例化

---

**第63题** 虚析构函数的作用是：
A. 提高析构效率
B. 当基类指针指向派生类对象并用delete释放时，正确调用派生类析构函数
C. 禁止析构函数被调用
D. 让析构函数可以被重载

---

**第64题** 以下关于静态联编和动态联编的描述，正确的是：
A. 静态联编在运行时决定调用的函数
B. 动态联编在编译时决定调用的函数
C. 静态联编效率高但灵活性差
D. 动态联编通过函数重载实现

---

**第65题** 参数多态的实现方式是：
A. 函数重载
B. 类型转换
C. 模板
D. 虚函数

---

### ● 运算符重载（66-70 题）

**第66题** 以下运算符不能重载的是：
A. `+`
B. `-`
C. `::`
D. `<<`

---

**第67题** 运算符重载为成员函数时，参数个数为：
A. 原操作数个数
B. 原操作数个数 - 1
C. 原操作数个数 + 1
D. 0

---

**第68题** 运算符重载为友元函数时，参数个数为：
A. 原操作数个数
B. 原操作数个数 - 1
C. 原操作数个数 + 1
D. 0

---

**第69题** 以下运算符重载规则，错误的是：
A. 不能改变运算符的优先级
B. 不能改变操作数个数
C. 可以发明新的运算符
D. 至少一个操作数是自定义类型

---

**第70题** 如果用友元函数重载 `operator+` 实现 `c1 + c2`（两个Complex对象），函数原型是：
A. `Complex operator+(Complex &c);`
B. `friend Complex operator+(const Complex &a, const Complex &b);`
C. `Complex operator+(Complex &c1, Complex &c2);`
D. `friend Complex operator+();`

---

## 二、填空题（共 30 题）

**第71题** 在面向对象方法中，对象 = \_\_\_\_\_\_ + \_\_\_\_\_\_。

---

**第72题** 类与对象的关系犹如 \_\_\_\_\_\_ 与 \_\_\_\_\_\_。

---

**第73题** 构造函数名称必须与 \_\_\_\_\_\_ 相同，且没有 \_\_\_\_\_\_ 类型。

---

**第74题** 析构函数名由 \_\_\_\_\_\_ 加类名组成，没有参数，没有返回值，且不能 \_\_\_\_\_\_。

---

**第75题** 如果没有显式定义构造函数，系统会自动生成一个 \_\_\_\_\_\_ 构造函数。

---

**第76题** this 指针隐含于每一个类的 \_\_\_\_\_\_ 中，指向 \_\_\_\_\_\_。

---

**第77题** 静态数据成员必须在 \_\_\_\_\_\_ 初始化，且要在对象生成之\_\_\_\_\_\_。

---

**第78题** 静态成员函数没有 \_\_\_\_\_\_ 指针，只能访问本类中的 \_\_\_\_\_\_ 成员。

---

**第79题** 友元关系是 \_\_\_\_\_\_ 向的，不具有 \_\_\_\_\_\_ 性，也不能被 \_\_\_\_\_\_。

---

**第80题** 友元是C++提供的一种破坏 \_\_\_\_\_\_ 和 \_\_\_\_\_\_ 的机制，好比"墙上开的小孔"。

---

**第81题** 常量引用的声明格式为：\_\_\_\_\_\_ 类型说明符 \_\_\_\_\_\_ 引用名。

---

**第82题** `const int *p` 声明的是 \_\_\_\_\_\_ 的指针；`int * const p` 声明的是 \_\_\_\_\_\_。

---

**第83题** 继承的目的是实现 \_\_\_\_\_\_，派生的目的是实现 \_\_\_\_\_\_。

---

**第84题** 派生类构造函数的执行顺序：先 \_\_\_\_\_\_，再 \_\_\_\_\_\_，后 \_\_\_\_\_\_。

---

**第85题** 在公有继承中，基类的 public 成员在派生类中仍为 \_\_\_\_\_\_；基类的 protected 成员在派生类中为 \_\_\_\_\_\_；基类的 private 成员 \_\_\_\_\_\_。

---

**第86题** 多态性是指发出同样的消息被不同类型的对象接收时可能导致 \_\_\_\_\_\_ 的行为。

---

**第87题** 实现动态多态需要三个条件：① 基类中声明 \_\_\_\_\_\_ 函数；② 类之间存在 \_\_\_\_\_\_ 关系；③ 使用 \_\_\_\_\_\_ 指向派生类对象并调用该函数。

---

**第88题** 纯虚函数的一般定义形式为：`virtual 返回值类型 函数名(参数表) = \_\_\_\_\_\_;`

---

**第89题** 包含至少一个 \_\_\_\_\_\_ 的类称为抽象类。抽象类 \_\_\_\_\_\_（能/不能）创建对象，但 \_\_\_\_\_\_（能/不能）声明指针和引用。

---

**第90题** 抽象类的作用是为类族提供统一的 \_\_\_\_\_\_。

---

**第91题** 不能重载的四个运算符是：\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_。（口诀：点、星、域、问）

---

**第92题** 运算符重载的实质是 \_\_\_\_\_\_ 的重载。关键字为 \_\_\_\_\_\_。

---

**第93题** `cerr` 是 \_\_\_\_\_\_ 输出流，\_\_\_\_\_\_（有/无）缓冲；`clog` 是 \_\_\_\_\_\_ 输出流，\_\_\_\_\_\_（有/无）缓冲。

---

**第94题** 读操作在流中被称为 \_\_\_\_\_\_（提取/插入），写操作被称为 \_\_\_\_\_\_。

---

**第95题** 派生类生成过程包括三个步骤：\_\_\_\_\_\_ 基类成员、\_\_\_\_\_\_ 继承方式、\_\_\_\_\_\_ 自己的成员。

---

**第96题** UML 的中文全称是 \_\_\_\_\_\_，它是一种 \_\_\_\_\_\_ 的面向对象建模语言。

---

**第97题** 在 UML 中，表示继承关系的是 \_\_\_\_\_\_ 关系。

---

**第98题** 深拷贝与浅拷贝的关键区别在于：当数据成员是 \_\_\_\_\_\_ 类型时，深拷贝会复制指针所指的对象本身，而不是复制 \_\_\_\_\_\_。

---

**第99题** 析构函数的调用顺序与构造函数 \_\_\_\_\_\_（相同/相反）。

---

**第100题** 内联函数声明时使用关键字 \_\_\_\_\_\_，编译时在调用处用函数体进行 \_\_\_\_\_\_。
