# 🧪 面向对象程序设计 补充卷

> 📌 **题型分布：** 选择题 10题（20分）| 填空题 15空（15分）| 简答题 3题（15分）| 看程序写结果 2题（10分）
>
> 📌 **满分：60分**
>
> 📌 **说明：** 本试卷覆盖3套模拟题和综合大卷子**都没有考到**的内容，基于教材《C++语言程序设计（第5版）》的知识点
>
> 📌 **覆盖内容：** 虚基类、类型兼容规则、RTTI、类型转换、C++11新特性、多文件结构、struct/union/enum、输入流深入

---

## 一、选择题（共10题，每题2分，共20分）

### ● 虚基类与类型兼容规则（第1-4题）

**1.1** 以下菱形继承中，使用虚基类的目的是：
```cpp
class A { public: int a; };
class B : virtual public A {};
class C : virtual public A {};
class D : public B, public C {};
```
A. 提高运行效率
B. 解决多重继承中基类被重复继承的问题
C. 实现多态
D. 提高代码可读性

**1.2** 关于类型兼容规则，以下说法错误的是：
A. 派生类对象可以赋值给基类对象（发生切片）
B. 派生类对象可以初始化基类的引用
C. 派生类对象的地址可以赋给指向基类的指针
D. 基类对象可以赋值给派生类对象

**1.3** 类型兼容规则中的"对象切片"是指：
A. 派生类对象赋值给基类对象时，派生类特有的成员被"切掉"
B. 基类对象赋值给派生类对象时，基类成员被"切掉"
C. 对象在内存中被分割成多块
D. 构造函数执行过程中的内存分配

**1.4** 以下代码执行后，`b` 的值为：
```cpp
class B { public: int x; B(int v) : x(v) {} };
class D : public B { public: int y; D(int v1, int v2) : B(v1), y(v2) {} };
D d(1, 2);
B b = d;
```
A. `1`
B. `2`
C. 不确定
D. 编译错误

---

### ● RTTI与类型转换（第5-7题）

**1.5** `dynamic_cast` 的使用条件是：
A. 被转换的类必须有虚函数
B. 被转换的类必须有默认构造函数
C. 任意类都可以使用
D. 必须有public继承关系

**1.6** 以下哪个类型转换用于去除const属性？
A. `static_cast`
B. `dynamic_cast`
C. `const_cast`
D. `reinterpret_cast`

**1.7** `typeid` 运算符返回的信息是：
A. 变量的地址
B. 变量的值
C. 变量的类型信息
D. 变量的大小

---

### ● 结构体/联合体/枚举/C++11（第8-10题）

**1.8** 关于C++中的 `union`（联合体），以下说法正确的是：
A. 所有成员各自独立占据内存
B. 所有成员共享同一块内存空间，大小为最大成员的大小
C. 联合体中不能有成员函数
D. 联合体的大小是所有成员大小之和

**1.9** 以下关于 `enum class`（C++11限定作用域枚举）的特点，错误的是：
A. 枚举值不会污染全局命名空间
B. 枚举值可以隐式转换为整数
C. 访问时必须使用 `枚举名::值` 的形式
D. 可以指定底层数据类型

**1.10** `auto` 关键字的作用是：
A. 声明自动存储期变量
B. 让编译器自动推断变量类型
C. 声明全局变量
D. 声明静态变量

---

## 二、填空题（共15空，每空1分，共15分）

**2.1** 菱形继承问题的解决方案是使用 \_\_\_\_\_\_ 继承，关键字为 \_\_\_\_\_\_。
**2.2** 类型兼容规则中，基类的 \_\_\_\_\_\_ 或 \_\_\_\_\_\_ 可以指向派生类对象。
**2.3** 安全的向下类型转换使用 \_\_\_\_\_\_，失败时返回 \_\_\_\_\_\_（针对指针转换）。
**2.4** 不能重载的四种类型转换是：\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_\_、\_\_\_\_\_\_。
**2.5** C++中联合体（union）的所有成员共享 \_\_\_\_\_\_ 内存空间，其大小为 \_\_\_\_\_\_ 的大小。
**2.6** 在C++中，`enum class` 的枚举值 \_\_\_\_\_\_（会/不会）污染全局命名空间。
**2.7** `auto` 关键字让编译器自动推断 \_\_\_\_\_\_ 类型。
**2.8** 输入流中读单个字符用 \_\_\_\_\_\_ 成员函数，读一行用 \_\_\_\_\_\_ 成员函数。
**2.9** 文件打开模式 `ios::app` 表示 \_\_\_\_\_\_。
**2.10** `setw(n)` 的作用是 \_\_\_\_\_\_，需要包含头文件 \_\_\_\_\_\_。

---

## 三、简答题（共3题，每题5分，共15分）

**3.1** 什么是虚基类？请结合菱形继承（钻石继承）问题说明虚基类的作用和虚基类构造函数的调用规则。

**3.2** 什么是类型兼容规则？它包含哪三种具体的赋值兼容方式？请举例说明。

**3.3** 请简述C++四种类型转换（`static_cast`、`dynamic_cast`、`const_cast`、`reinterpret_cast`）各自的用途和适用场景。

---

## 四、看程序写结果（共2题，每题5分，共10分）

### 4.1 虚基类构造顺序

```cpp
#include <iostream>
using namespace std;

class A {
public:
    A(int x) { cout << "A(" << x << ")" << endl; }
};

class B : virtual public A {
public:
    B(int x, int y) : A(x) { cout << "B(" << y << ")" << endl; }
};

class C : virtual public A {
public:
    C(int x, int z) : A(x) { cout << "C(" << z << ")" << endl; }
};

class D : public B, public C {
public:
    D(int x, int y, int z) : A(x), B(x, y), C(x, z) {
        cout << "D()" << endl;
    }
};

int main() {
    D d(1, 2, 3);
    return 0;
}
```

**运行结果：**

```
________________
________________
________________
________________
```

---

### 4.2 RTTI与类型转换

```cpp
#include <iostream>
#include <typeinfo>
using namespace std;

class Base {
public:
    virtual void show() { cout << "Base" << endl; }
};

class Derived : public Base {
public:
    void show() override { cout << "Derived" << endl; }
    void onlyDerived() { cout << "Only Derived" << endl; }
};

void test(Base* p) {
    cout << typeid(*p).name() << ": ";
    Derived* d = dynamic_cast<Derived*>(p);
    if (d) {
        d->onlyDerived();
    } else {
        cout << "Not Derived" << endl;
    }
}

int main() {
    Base b;
    Derived d;
    test(&b);
    test(&d);
    return 0;
}
```

**运行结果：**

```
________________
________________
________________
```

---

> **做完再看答案 → 详见《补充卷_答案与解析.md》**
