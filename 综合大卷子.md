# 🧪 面向对象程序设计 综合大卷子

> 📌 **题型分布：** 选择题 70题（70分）| 填空题 36空（36分）| 简答题 12题（60分）| 看程序写结果 9题（45分）
> 
> 📌 **满分：211分**
> 
> 📌 **范围：** 覆盖绪论、类与对象、构造函数与析构函数、拷贝构造、对象组合、静态成员、友元、const保护、this指针、**引用**、继承与派生、多态性、虚函数、抽象类、运算符重载、流类库、UML等全部知识点

---

## 一、选择题（共65题，每题1分，共65分）

### ● 绪论与基本概念（第1-8题）

**1.1** 面向过程程序设计关注的是：
A. 谁来做
B. 怎么做
C. 对象之间的关系
D. 数据封装

**1.2** 以下哪项不是面向对象程序设计的基本特征？
A. 抽象性
B. 封装性
C. 递归性
D. 多态性

**1.3** 类与对象的关系，最恰当的比喻是：
A. 汽车与轮胎
B. 蓝图与房屋
C. 树干与树枝
D. 河流与水滴

**1.4** "一个接口，多种状态"描述的是：
A. 继承
B. 封装
C. 多态
D. 抽象

**1.5** 下面关于消息的描述，错误的是：
A. 同一对象可接收不同形式的多个消息
B. 相同形式的消息发送给不同对象，响应可以截然不同
C. 消息的发送可以不考虑具体的接收者
D. 对象必须对收到的消息做出响应

**1.6** 面向对象方法的优点不包括：
A. 可重用性
B. 可维护性
C. 可扩充性
D. 运行速度更快

**1.7** 在面向对象方法中，"对象"由哪两部分构成？
A. 函数和变量
B. 属性和行为
C. 输入和输出
D. 基类和派生类

**1.8** 以下哪个是面向过程的代表性语言？
A. C++
B. Java
C. C
D. Python

---

### ● 类与对象（第9-20题）

**1.9** 类中私有成员的特点，正确的是：
A. 类内类外均可访问
B. 只有本类成员函数和友元可以访问
C. 派生类可以访问
D. 任何函数都可以访问

**1.10** 在类中，用于声明类与外部的接口的关键字是：
A. private
B. protected
C. public
D. static

**1.11** 关于构造函数，下列说法正确的是：
A. 构造函数可以有返回值
B. 构造函数不能被重载
C. 构造函数名必须与类名相同
D. 构造函数必须显式定义

**1.12** 如果没有显式定义构造函数，系统会：
A. 报错
B. 自动生成一个带参数的构造函数
C. 自动生成一个默认构造函数，不做初始化
D. 随机生成一个构造函数

**1.13** 以下关于析构函数的描述，错误的是：
A. 析构函数没有参数
B. 析构函数不能重载
C. 析构函数可以有返回值
D. 一个类有且仅有一个析构函数

**1.14** 析构函数名由什么组成？
A. `@` + 类名
B. `~` + 类名
C. `!` + 类名
D. `#` + 类名

**1.15** 构造函数初始化列表的写法是：
A. `Date(int y, int m, int d) : year(y), month(m), day(d) {}`
B. `Date(int y, int m, int d) { year=y; month=m; day=d; }`
C. `Date(int y, int m, int d) = year(y), month(m), day(d)`
D. `Date(int y, int m, int d) :: year(y), month(m), day(d)`

**1.16** 当用 `delete` 释放动态对象时，系统会：
A. 释放内存，但不调用析构函数
B. 先调用析构函数，再释放内存
C. 只调用析构函数
D. 什么也不做

**1.17** 静态局部对象（static属性）在什么时候调用析构函数？
A. 函数调用结束时
B. 对象被删除时
C. main函数结束或调用exit时
D. 永远不会调用

**1.18** 关于对象的组合，内嵌对象构造函数的调用顺序是：
A. 按初始化列表中的顺序
B. 按在类中声明顺序
C. 按参数列表顺序
D. 随机顺序

**1.19** 组合类的析构函数调用顺序与构造函数：
A. 完全相同
B. 完全相反
C. 无关
D. 随机

**1.20** 前向引用声明可以用于：
A. 声明该类的对象
B. 在内联成员函数中使用该类的对象
C. 声明该类的指针或引用
D. 定义该类的内联成员函数

---

### ● 拷贝构造函数（第21-24题）

**1.21** 拷贝构造函数的形参必须是：
A. 本类对象
B. 本类对象的引用
C. 本类对象的指针
D. 任意类型

**1.22** 以下哪种情况不会调用拷贝构造函数？
A. 用对象初始化另一个对象：`Student s2 = s1;`
B. 对象作为函数参数（值传递）
C. 函数返回对象（值返回）
D. 用对象指针初始化另一个对象指针：`Student* p2 = &s1;`

**1.23** 当类中含有指针类型的数据成员时，通常需要：
A. 使用默认拷贝构造函数
B. 自定义拷贝构造函数实现深拷贝
C. 删除拷贝构造函数
D. 将指针成员设为静态

**1.24** 浅拷贝可能导致的问题是：
A. 编译错误
B. 运行时double free（重复释放内存）
C. 数据成员无法初始化
D. 程序无法启动

---

### ● 静态成员（第25-29题）

**1.25** 静态数据成员初始化的正确方式是：
A. 在构造函数中初始化
B. 在类定义中直接赋值
C. 在类外，对象生成之前初始化
D. 在main函数中初始化

**1.26** 静态成员函数的特点是：
A. 有this指针
B. 可以访问非静态成员
C. 没有this指针，只能访问静态成员
D. 只能通过对象名调用

**1.27** 静态数据成员属于：
A. 每一个对象各自单独拥有
B. 整个类，所有对象共享    
C. 主函数
D. 全局空间

**1.28** 静态成员函数不能访问非静态成员，原因是：
A. 静态成员函数没有this指针
B. 静态成员函数在编译时执行
C. 静态成员函数没有参数
D. 静态成员函数是全局的

**1.29** 静态数据成员在内存中的存储位置：
A. 和普通数据成员一样在对象中
B. 单独分配存储区域
C. 在栈中
D. 在寄存器中

---

### ● 友元（第30-33题）

**1.30** 下面关于友元的描述，错误的是：
A. 友元可以访问类的私有成员
B. 友元关系可以被继承
C. 友元关系是单向的
D. 友元关系不具有传递性

**1.31** 友元的类型包括：
A. 友元函数和友元类
B. 友元函数和友元对象
C. 友元类和友元对象
D. 友元函数和友元指针

**1.32** 友元的主要缺点是：
A. 增加代码量
B. 破坏封装性和数据隐藏性
C. 降低运行效率
D. 增加编译时间

**1.33** 若A是B的友元类，C是A的友元类，则：
A. C一定是B的友元类（传递性）
B. C不一定是B的友元类（无传递性）
C. C一定不是B的友元类
D. B一定是C的友元类（双向性）

---

### ● const与this指针（第34-38题）

**1.34** `const int *p;` 声明的是：
A. 指向常量的指针（指针指向的值不可改）
B. 常量指针（指针本身不可改）
C. 指向指针的常量
D. 常量指针常量

**1.35** `int * const p = &x;` 声明的是：
A. 指向常量的指针
B. 常量指针（指针本身不能改变）
C. 指向指针的指针
D. 常量引用

**1.36** this 指针隐含于：
A. 全局函数中
B. 每一个类的非静态成员函数中
C. 仅静态成员函数中
D. 友元函数中

**1.37** 常量引用所引用的对象：
A. 可以被修改
B. 不能被修改
C. 可以重新赋值
D. 可以指向其他变量

**1.38** 关于this指针，以下说法正确的是：
A. this指针可以显式修改
B. 静态成员函数也有this指针
C. this指针指向当前操作的数据所属的对象
D. this指针的类型是void*

---

### ● 继承与派生（第39-47题）

**1.39** 继承的目的和派生的目的分别是：
A. 继承为代码重用，派生为功能扩充
B. 继承为功能扩充，派生为代码重用
C. 都是代码重用
D. 都是功能扩充

**1.40** 在公有继承中，基类的 public 成员在派生类中变为：
A. private
B. protected
C. public
D. 不可访问

**1.41** 在公有继承中，基类的 protected 成员在派生类中变为：
A. private
B. protected
C. public
D. 不可访问

**1.42** 在公有继承中，基类的 private 成员在派生类中：
A. 变为 public
B. 变为 protected
C. 保持 private
D. 不可直接访问（但确实存在于对象中）

**1.43** 派生类构造函数执行顺序是：
A. 先自己，再客人，后祖先
B. 先祖先，再客人，后自己
C. 先祖先，再自己，后客人
D. 先客人，再祖先，后自己

**1.44** 派生类的析构函数执行顺序是：
A. 基类 → 内嵌对象 → 派生类
B. 派生类 → 内嵌对象 → 基类
C. 内嵌对象 → 基类 → 派生类
D. 基类 → 派生类 → 内嵌对象

**1.45** 多继承时，基类构造函数的调用顺序是：
A. 按照派生类声明时继承的顺序（从左到右）
B. 按照初始化列表中出现的顺序
C. 按照基类名的大小顺序
D. 随机顺序

**1.46** 下面哪种关系适合用继承表示？
A. 学生-班级
B. 汽车-轮胎
C. 学生-研究生
D. 公司-员工

**1.47** 若基类声明了带参数的构造函数，则派生类必须：
A. 省略基类构造函数的调用
B. 在初始化列表中显式调用基类构造函数
C. 不定义构造函数
D. 使用默认构造函数

---

### ● 多态性与虚函数（第48-55题）

**1.48** 以下哪种多态是运行时多态？
A. 函数重载
B. 运算符重载
C. 包含多态（虚函数）
D. 参数多态（模板）

**1.49** 实现动态多态不需要以下哪个条件？
A. 基类中将函数声明为虚函数
B. 公有继承（is-a关系）
C. 使用派生类指针指向基类对象
D. 通过基类指针或引用调用虚函数

**1.50** 以下哪个函数不能声明为虚函数？
A. 析构函数
B. 普通成员函数
C. 构造函数
D. 以上都可以

**1.51** 纯虚函数的定义形式是：
A. `virtual 返回值 函数名(参数表) {}`
B. `virtual 返回值 函数名(参数表) = 0;`
C. `virtual 返回值 函数名(参数表);`
D. `返回值 函数名(参数表) = 0;`

**1.52** 关于抽象类，以下说法正确的是：
A. 可以创建抽象类的对象
B. 可以声明抽象类的指针或引用
C. 抽象类不能用作基类
D. 抽象类可以实例化

**1.53** 虚析构函数的作用是：
A. 提高析构效率
B. 当基类指针指向派生类对象并用delete释放时，正确调用派生类析构函数
C. 禁止析构函数被调用
D. 让析构函数可以被重载

**1.54** 关于虚函数，错误的是：
A. 构造函数不能是虚函数
B. 静态成员函数不能是虚函数
C. 内联函数不能是虚函数
D. 析构函数不能是虚函数

**1.55** 参数多态的实现方式是：
A. 函数重载
B. 类型转换
C. 模板
D. 虚函数

---

### ● 运算符重载（第56-60题）

**1.56** 以下运算符不能重载的是：
A. `+`
B. `-`
C. `::`
D. `<<`

**1.57** 运算符重载为成员函数时，参数个数为：
A. 原操作数个数
B. 原操作数个数 - 1
C. 原操作数个数 + 1
D. 0

**1.58** 运算符重载为友元函数时，参数个数为：
A. 原操作数个数
B. 原操作数个数 - 1
C. 原操作数个数 + 1
D. 0

**1.59** 以下运算符重载规则，错误的是：
A. 不能改变运算符的优先级
B. 不能改变操作数个数
C. 可以发明新的运算符
D. 至少一个操作数是自定义类型

**1.60** 如果用友元函数重载 `operator+` 实现 `c1 + c2`，函数原型是：
A. `Complex operator+(Complex &c);`
B. `friend Complex operator+(const Complex &a, const Complex &b);`
C. `Complex operator+(Complex &c1, Complex &c2);`
D. `friend Complex operator+();`

---

### ● UML（第61-65题）

**1.61** UML的中文全称是：
A. 统一编程语言
B. 统一建模语言
C. 通用标记语言
D. 通用编程语言

**1.62** UML是一种什么样的语言？
A. 编译型语言
B. 可视化的面向对象建模语言
C. 脚本语言
D. 数据库查询语言

**1.63** 在UML中，表示继承关系的术语是：
A. 依赖关系
B. 关联关系
C. 泛化关系
D. 实现关系

**1.64** 以下哪种关系适合用UML中的聚合关系表示？
A. 学生-研究生（is-a）
B. 汽车-轮胎（has-a）
C. 形状-圆形
D. 动物-狗

**1.65** UML中的"泛化"关系对应C++中的什么机制？
A. 封装
B. 继承
C. 多态
D. 模板

---

### ● 引用（第66-70题）

**1.66** 关于引用，以下说法正确的是：
A. 引用是一个独立的变量，有自己独立的内存空间
B. 引用是变量的别名，对引用的操作等价于对被引用变量的操作
C. 引用可以不初始化
D. 引用可以重新绑定到另一个变量

**1.67** 以下函数参数传递方式中，可以直接修改实参且语法最简洁的是：
A. 值传递 `void func(int a)`
B. 指针传递 `void func(int* a)`
C. 引用传递 `void func(int& a)`
D. 以上都不行

**1.68** 关于引用与指针的区别，错误的是：
A. 引用必须初始化，指针可以不初始化
B. 引用绑定后不能改变，指针可以改变指向
C. 引用有空值，指针有nullptr
D. 引用不需要解引用操作，指针需要`*`解引用

**1.69** `const int& ref = x;` 以下操作正确的是：
A. `ref = 10;`
B. `cin >> ref;`
C. `cout << ref;`
D. `ref++;`

**1.70** 若函数返回引用，以下说法错误的是：
A. 返回引用可以避免拷贝，提高效率
B. 不能返回局部变量的引用
C. 返回引用可以作为左值
D. 返回引用一定比返回值更安全

---

## 二、填空题（共36空，每空1分，共36分）

**2.1** 在面向对象方法中，对象 = \_\_\_\_\_\_ + \_\_\_\_\_\_。

**2.2** 类与对象的关系犹如 \_\_\_\_\_\_ 与 \_\_\_\_\_\_。

**2.3** 构造函数名称必须与 \_\_\_\_\_\_ 相同，且没有 \_\_\_\_\_\_ 类型。

**2.4** 析构函数名由 \_\_\_\_\_\_ 加类名组成，不能 \_\_\_\_\_\_ ，没有返回值，没有参数。

**2.5** this 指针隐含于每一个类的 \_\_\_\_\_\_ 中，指向 \_\_\_\_\_\_。

**2.6** 静态数据成员必须在 \_\_\_\_\_\_ 初始化，且要在对象生成之 \_\_\_\_\_\_。

**2.7** 静态成员函数没有 \_\_\_\_\_\_ 指针，只能访问本类中的 \_\_\_\_\_\_ 成员。

**2.8** 友元关系是 \_\_\_\_\_\_ 向的，不具有 \_\_\_\_\_\_ 性，也不能被 \_\_\_\_\_\_。

**2.9** 继承的目的是实现 \_\_\_\_\_\_，派生的目的是实现 \_\_\_\_\_\_。

**2.10** 派生类构造函数的执行顺序：先 \_\_\_\_\_\_，再 \_\_\_\_\_\_，后 \_\_\_\_\_\_。（口诀：先祖先，再客人，后自己）

**2.11** 多态性是指发出同样的消息被不同类型的对象接收时可能导致 \_\_\_\_\_\_ 的行为。

**2.12** 包含至少一个 \_\_\_\_\_\_ 的类称为抽象类。抽象类 \_\_\_\_\_\_（能/不能）创建对象，但 \_\_\_\_\_\_（能/不能）声明指针和引用。

**2.13** 不能重载的四个运算符是：\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_。（口诀：点、星、域、问）

**2.14** `cerr` 是 \_\_\_\_\_\_ 输出流，\_\_\_\_\_\_（有/无）缓冲；`clog` 是 \_\_\_\_\_\_ 输出流，\_\_\_\_\_\_（有/无）缓冲。

**2.15** 读操作在流中被称为 \_\_\_\_\_\_，写操作被称为 \_\_\_\_\_\_。

**2.16** UML的中文全称是 \_\_\_\_\_\_，它是一种 \_\_\_\_\_\_ 的面向对象建模语言。

**2.17** 在UML中，表示继承关系的是 \_\_\_\_\_\_ 关系。UML中包含的三种基本组成部分是：事物、\_\_\_\_\_\_ 和图。

**2.18** 引用是变量的 \_\_\_\_\_\_，声明引用时必须 \_\_\_\_\_\_。
**2.19** 函数参数使用引用传递时，形参与实参共享 \_\_\_\_\_\_，对形参的修改会 \_\_\_\_\_\_（影响/不影响）实参。
**2.20** 常量引用声明的格式为：\_\_\_\_\_\_ 类型说明符 &引用名；常量引用引用的对象 \_\_\_\_\_\_（能/不能）被修改。

---

## 三、简答题（共12题，每题5分，共60分）

**3.1** 请简述面向对象的四大基本特征，并简要说明每个特征的含义。

**3.2** 什么是构造函数？它有哪些特点？（至少列出5条）

**3.3** 什么是拷贝构造函数？在哪些情况下会被调用？当类中含有指针类型的数据成员时，为什么通常需要自定义拷贝构造函数？

**3.4** 什么是静态成员？静态数据成员和静态成员函数各有什么特点？

**3.5** 什么是友元？友元有哪些优缺点？友元关系有哪些特性？

**3.6** 请比较 public、protected、private 三种访问权限的区别，并各举一个访问场景。

**3.7** 实现动态多态（运行时多态）需要哪三个条件？请结合虚函数说明。

**3.8** 什么是纯虚函数？什么是抽象类？抽象类有什么特点和作用？

**3.9** 什么是静态联编？什么是动态联编？它们有什么区别？各通过什么方式实现？

**3.10** 运算符重载的规则有哪些？（至少列出6条）运算符重载的两种形式（成员函数和友元函数）有什么区别？

**3.11** 什么是UML？它包含哪三种基本组成部分？UML中的四种关系分别是什么？其中哪种关系对应C++中的继承机制？

**3.12** 请比较值传递、指针传递、引用传递三种函数参数传递方式的区别，并说明const引用作参数的优势。

---

## 四、看程序写结果（共9题，每题5分，共45分）

### 4.1 虚函数与动态多态

```cpp
#include <iostream>
using namespace std;

class Base {
public:
    virtual void show() {
        cout << "Base" << endl;
    }
};

class Derived : public Base {
public:
    void show() override {
        cout << "Derived" << endl;
    }
};

int main() {
    Base* p;
    Base b;
    Derived d;
    p = &b;
    p->show();
    p = &d;
    p->show();
    return 0;
}
```

**运行结果：**

```
____________
____________
```

---

### 4.2 静态成员与构造函数

```cpp
#include <iostream>
using namespace std;

class Student {
private:
    static int total;
    int id;
public:
    Student() { total++; id = total; }
    void show() {
        cout << "ID: " << id << ", Total: " << total << endl;
    }
};

int Student::total = 0;

int main() {
    Student s1, s2, s3;
    s1.show();
    s2.show();
    s3.show();
    return 0;
}
```

**运行结果：**

```
____________
____________
____________
```

---

### 4.3 继承构造与析构顺序

```cpp
#include <iostream>
using namespace std;

class Base {
public:
    Base() { cout << "Base constructor" << endl; }
    ~Base() { cout << "Base destructor" << endl; }
};

class Derived : public Base {
public:
    Derived() { cout << "Derived constructor" << endl; }
    ~Derived() { cout << "Derived destructor" << endl; }
};

int main() {
    Derived d;
    return 0;
}
```

**运行结果：**

```
____________
____________
____________
____________
```

---

### 4.4 非虚析构函数的缺陷

```cpp
#include <iostream>
using namespace std;

class Base {
public:
    Base() { cout << "Base constructor" << endl; }
    ~Base() { cout << "Base destructor" << endl; }
};

class Derived : public Base {
public:
    Derived() { cout << "Derived constructor" << endl; }
    ~Derived() { cout << "Derived destructor" << endl; }
};

int main() {
    Base* p = new Derived();
    delete p;
    return 0;
}
```

**运行结果：**

```
____________
____________
____________
```

---

### 4.5 多态数组

```cpp
#include <iostream>
using namespace std;

class Shape {
public:
    virtual void draw() {
        cout << "Shape" << endl;
    }
};

class Circle : public Shape {
public:
    void draw() override {
        cout << "Circle" << endl;
    }
};

class Rectangle : public Shape {
public:
    void draw() override {
        cout << "Rectangle" << endl;
    }
};

int main() {
    Shape* shapes[3];
    Shape s;
    Circle c;
    Rectangle r;
    shapes[0] = &s;
    shapes[1] = &c;
    shapes[2] = &r;
    for (int i = 0; i < 3; i++) {
        shapes[i]->draw();
    }
    return 0;
}
```

**运行结果：**

```
____________
____________
____________
```

---

### 4.6 继承中的protected成员访问

```cpp
#include <iostream>
using namespace std;

class Base {
protected:
    int a;
public:
    Base(int x) : a(x) {}
    void show() { cout << "Base a=" << a << endl; }
};

class Derived : public Base {
private:
    int b;
public:
    Derived(int x, int y) : Base(x), b(y) {}
    void show() {
        cout << "Derived a=" << a << ", b=" << b << endl;
    }
};

int main() {
    Base base(10);
    Derived derived(20, 30);
    base.show();
    derived.show();
    return 0;
}
```

**运行结果：**

```
________________
________________
```

---

### 4.7 运算符重载（成员函数）

```cpp
#include <iostream>
using namespace std;

class Complex {
private:
    double real, imag;
public:
    Complex(double r = 0, double i = 0) : real(r), imag(i) {}
    Complex operator+(const Complex& c) {
        return Complex(real + c.real, imag + c.imag);
    }
    void display() {
        cout << real << "+" << imag << "i" << endl;
    }
};

int main() {
    Complex c1(2.5, 3.0), c2(1.5, 2.0);
    Complex c3 = c1 + c2;
    c1.display();
    c2.display();
    c3.display();
    return 0;
}
```

**运行结果：**

```
________________
________________
________________
```

---

### 4.8 虚析构函数的作用

```cpp
#include <iostream>
using namespace std;

class Base {
public:
    Base() { cout << "Base()" << endl; }
    virtual ~Base() { cout << "~Base()" << endl; }
};

class Derived : public Base {
public:
    Derived() { cout << "Derived()" << endl; }
    ~Derived() override { cout << "~Derived()" << endl; }
};

int main() {
    Base* p = new Derived();
    delete p;
    return 0;
}
```

**运行结果：**

```
____________
____________
____________
____________
```

### 4.9 引用作参数与引用作返回值

```cpp
#include <iostream>
using namespace std;

int& getMax(int& a, int& b) {
    return (a > b) ? a : b;
}

void swapRef(int& x, int& y) {
    int t = x;
    x = y;
    y = t;
}

int main() {
    int m = 10, n = 20;
    cout << "m=" << m << ", n=" << n << endl;
    
    getMax(m, n) = 100;
    cout << "After getMax: m=" << m << ", n=" << n << endl;
    
    swapRef(m, n);
    cout << "After swapRef: m=" << m << ", n=" << n << endl;
    
    return 0;
}
```

**运行结果：**

```
________________
________________
________________
```

---

> **做完再看答案 → 详见《综合大卷子_答案与解析.md》**
